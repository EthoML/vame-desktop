{
    "title": "Motifs Videos",
    "type": "object",
    "properties": {
        "length_of_motif_video": {
            "type": "integer",
            "title": "Max Video Length (frames)",
            "description": "Maximum number of frames per motif video clip.",
            "default": 1000,
            "minimum": 1
        },
        "overwrite": {
            "type": "boolean",
            "title": "Overwrite Existing Videos",
            "description": "If true, overwrite any existing videos",
            "default": false
        }
    },
    "required": [
        "overwrite"
    ]
}