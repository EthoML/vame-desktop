{
    "properties": {
        "name": {
            "type": "string",
            "title": "Project Name",
			"description": "Please do not have any spaces in the name"
        },
        "videos": {
            "type": "array",
            "title": "Videos",
			"description": "Load labeled videos, NOTE: video file name needs to match CSV file name",
            "items": {
                "type": "file",
                "accept": "video/*"
            }
        },
        "csvs": {
            "type": "array",
            "title": "CSV Files",
			"description": "Load pose estimation data, NOTE: csv file name needs to match video file name",
            "items": {
                "type": "file",
                "accept": [".csv"]
            }
        },
        "videotype": {
            "type": "string",
            "title": "Video Type",
			"description": "mp4, avi, etc."
        }
    },
    "required": [ "name", "videos", "csvs", "videotype" ]
}