{
    "title": "Create VAME Project",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "title": "Project Name",
            "description": "Name of the project. No spaces allowed.",
            "allow-spaces": false
        },
        "pes_paths": {
            "type": "string",
            "format": "file",
            "description": "Pose estimation data files. Supported formats: CSV, SLP, H5 (DeepLabCut / SLEAP / LightningPose), NWB, or movement NetCDF (.nc). All files must be the same type.",
            "title": "Pose Estimation Files",
            "accept": [
                ".csv",
                ".slp",
                ".h5",
                ".nwb",
                ".nc"
            ],
            "multiple": true
        },
        "processing_module_key": {
            "type": "string",
            "title": "NWB Processing Module",
            "description": "Name of the NWB processing module that holds the pose estimation data.",
            "default": "behavior",
            "visibleWhen": {
                "field": "pes_paths",
                "fileExtension": ".nwb"
            }
        },
        "pose_estimation_key": {
            "type": "string",
            "title": "NWB Pose Estimation Key",
            "description": "Name of the PoseEstimation object inside the processing module",
            "default": "PoseEstimation",
            "visibleWhen": {
                "field": "pes_paths",
                "fileExtension": ".nwb"
            }
        },
        "videos": {
            "title": "Video Files (Optional)",
            "type": "string",
            "description": "Labeled videos corresponding to the pose estimation files (.mp4, .avi)",
            "format": "file",
            "accept": [
                ".mp4",
                ".avi"
            ],
            "multiple": true
        },
        "fps": {
            "type": "number",
            "title": "Frames per second",
            "description": "Required when no video is selected; otherwise read from the video.",
            "default": 30,
            "minimum": 1,
            "disabledWhen": {
                "field": "videos",
                "nonEmpty": true
            }
        },
        "project_random_state": {
            "type": "integer",
            "title": "Random seed",
            "description": "Seed for reproducibility",
            "minimum": 0
        }
    },
    "required": [
        "name",
        "pes_paths"
    ]
}