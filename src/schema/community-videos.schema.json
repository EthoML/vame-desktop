{
    "title": "Community Videos",
    "type": "object",
    "properties": {
        "parametrization": {
            "type": "string",
            "enum": ["hmm", "kmeans"]
        }
    },
    "required": [
        "parametrization"
    ]
}