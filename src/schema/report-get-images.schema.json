{
    "title": "Get Report Images",
    "type": "object",
    "properties": {
        "segmentation_algorithm": {
            "type": "string",
            "enum": [
                "hmm",
                "kmeans",
                "hmm_warmstart"
            ]
        },
        "session": {
            "type": "string",
            "title": "Session",
            "description": "Select the session to retrieve report images from"
        }
    },
    "required": [
        "segmentation_algorithm",
        "session"
    ]
}