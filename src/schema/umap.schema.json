{
  "title": "UMAP Visualization",
  "type": "object",
  "properties": {
      "parametrization": {
          "type": "string",
          "enum": ["hmm", "kmeans"],
          "default": "hmm"
      }
  },
  "required": [
      "parametrization"
  ]
}