{
    "title": "Get UMAP Images",
    "type": "object",
    "properties": {
        "segmentation_algorithm": {
            "type": "string",
            "enum": [
                "hmm",
                "kmeans",
                "hmm_warmstart"
            ]
        }
    },
    "required": [
        "segmentation_algorithm"
    ]
}