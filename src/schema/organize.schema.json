{
    "title": "Data Organization Parameters",
    "properties": {
        "pose_ref_index": {
            "type": "array",
            "title": "Reference Points",
            "description": "Indices of the reference points in the pose vector.",
            "items": {
                "type": "integer"
            },
            "minItems": 2,
            "maxItems": 2,
            "default": [ 0, 5 ]
        },
        "crop_size": {
            "type": "array",
            "description": "Size to crop the video.",
            "items": {
                "type": "integer"
            },
            "minItems": 2,
            "maxItems": 2,
            "default": [300, 300]
        },
        "use_video": {
            "type": "boolean",
            "description": "Whether to use video to do the post alignment."
        },
        "video_format": {
            "type": "string",
            "description": "Format of the video file."
        },
        "check_video": {
            "type": "boolean",
            "description": "Whether to check the video file."
        },
        "check_parameter": {
            "type": "boolean",
            "description": "Whether to plot the z-scored data and the filtered data",
            "default": false
        }
    }
}