{
    "title": "Create Trainset Parameters",
    "properties": {
        "check_parameter": {
            "type": "boolean",
            "description": "(Optional) Whether to plot the z-scored data and the filtered data",
            "default": false
        }
    }
}