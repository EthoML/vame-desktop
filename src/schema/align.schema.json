{
    "title": "Egocentric Alignment Parameters",
    "properties": {
        "pose_ref_index": {
            "type": "array",
            "description": "Indices of the reference points in the pose vector.",
            "items": {
                "type": "integer"
            },
            "minItems": 2,
            "maxItems": 2,
            "default": [5,6]
        },
        "crop_size": {
            "type": "array",
            "description": "Size to crop the video.",
            "items": {
                "type": "integer"
            },
            "minItems": 2,
            "maxItems": 2,
            "default": [300, 300]
        },
        "use_video": {
            "type": "boolean",
            "description": "Whether to use video to do the post alignment."
        },
        "video_format": {
            "type": "string",
            "description": "Format of the video file."
        },
        "check_video": {
            "type": "boolean",
            "description": "Whether to check the video file."
        }
    }
}