name: vame-desktop
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - numpy
  - pip:
    - flask
    - flask_restx
    - flask-cors
    - pyinstaller==6.8.0
    - pyinstaller-hooks-contrib == 2024.6
    - vame-py=0.2.0

prefix: /opt/anaconda3/envs/vame-desktop