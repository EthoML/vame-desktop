name: vame-desktop
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - flask
    - flask_restx
    - flask-cors
    - pyinstaller
    # - vame @ git+https://github.com/EthoML/VAME.git@dev#vame # Not working with latest requirements.txt (as of 2024-05-10). Please install manually.

prefix: /opt/anaconda3/envs/vame-desktop